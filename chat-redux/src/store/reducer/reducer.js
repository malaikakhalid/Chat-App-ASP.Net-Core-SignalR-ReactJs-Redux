const INITIAL_STATE = {
    username: "Malaika",
    email: "malaika@hotmail.com",
    data : []
}

export default (state = INITIAL_STATE, action) => {
    // console.log("action", action)
 return state
}